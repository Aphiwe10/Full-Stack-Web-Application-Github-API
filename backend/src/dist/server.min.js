"use strict";var express=require("express"),dotenv=require("dotenv"),cors=require("cors"),githubController=require("./controllers/githubController"),errorMiddleware=require("./middlewares/errorMiddleware"),helmet=require("helmet");dotenv.config();var app=express(),PORT=process.env.PORT||5e3;app.use(cors()),app.use(express.json()),app.use(helmet()),app.get("/",function(e,r){r.send("Welcome to the GitHub API Backend")}),app.get("/api/github/search",githubController.searchUsers),app.get("/api/github/user/:username",githubController.getUserDetails),app.get("/api/github/user/:username/repos",githubController.getUserRepos),app.get("/api/github/commits",githubController.getRepoCommits),app.use(errorMiddleware),process.env.GITHUB_TOKEN||(console.error("Error: GITHUB_TOKEN is not set in the .env file."),process.exit(1)),app.listen(PORT,function(){console.log("Server is running on http://localhost:".concat(PORT))});
//# sourceMappingURL=server.min.js.map
